#include <stdio.h>
/**
 *This is now a program that really prints a lie with printf function
 *return:0 (success)
 */
int main(void)
{printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
