#include <stdio.h>
/**
 * This is now a program that really prints a lin e using the Printf thingy function kkk
 * return:0 (success)
 */
int main(void)
{printf("with proper grammar, but the outcome is a piece of art,\n");
return(0);
}
