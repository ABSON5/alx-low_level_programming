#include < stdio.h>
/**
 * main - This program prints a line with usage of the puts function
 * Return:0 (will be successful)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
